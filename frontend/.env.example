# Backend API base URL. Override for staging/production deployments.
VITE_API_BASE_URL=http://localhost:8000

# Temporary auth maintenance switch.
# When true, /login and /signup show the maintenance page.
VITE_AUTH_MAINTENANCE_MODE=false
