# =============================================================================
# VocalGuard — Continuous Integration
# =============================================================================
# Triggers: push to main/develop, all pull requests
# Jobs:     lint → build (frontend + backend in parallel)
#
# Design principles:
#   - Fail fast: lint runs first, builds only start if lint passes
#   - Cache aggressively: pip + npm caches cut ~60s per run
#   - Artifact handoff: build outputs are uploaded for the deploy workflow
#   - No secrets in CI: this workflow runs on forks safely
# =============================================================================

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# ---------------------------------------------------------------------------
# Shared environment
# ---------------------------------------------------------------------------
env:
  NODE_VERSION: "20"
  PYTHON_VERSION: "3.11"

jobs:
  # =========================================================================
  # 1. Lint — fast gate before expensive builds
  # =========================================================================
  lint:
    name: "Lint"
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ----- Frontend lint -----
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: ESLint
        run: npm run lint
        working-directory: frontend

      # ----- Backend lint -----
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install linting tools
        run: pip install ruff==0.9.7

      - name: Ruff lint (check)
        run: ruff check backend/ --output-format=github

      - name: Ruff format (check)
        run: ruff format --check backend/

  # =========================================================================
  # 2. Build frontend
  # =========================================================================
  build-frontend:
    name: "Build Frontend"
    runs-on: ubuntu-latest
    needs: lint
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: frontend

      - name: Build production bundle
        run: npm run build
        working-directory: frontend
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL || 'https://api.vocalguard.app' }}

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 7
          if-no-files-found: error

  # =========================================================================
  # 3. Build + test backend
  # =========================================================================
  build-backend:
    name: "Build Backend"
    runs-on: ubuntu-latest
    needs: lint
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: Verify imports
        run: |
          cd backend
          python -c "
          import fastapi
          import uvicorn
          import pydantic
          print('✅ Core imports OK')
          print(f'   FastAPI {fastapi.__version__}')
          print(f'   Pydantic {pydantic.__version__}')
          "

      # Placeholder for future test suite — uncomment when tests exist
      # - name: Run pytest
      #   run: |
      #     pip install pytest pytest-cov pytest-asyncio httpx
      #     cd backend
      #     pytest tests/ -v --tb=short --cov=. --cov-report=xml
      #   env:
      #     FIREBASE_WEB_API_KEY: "test-key-ci"

      # - name: Upload coverage
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: coverage-report
      #     path: backend/coverage.xml

  # =========================================================================
  # 4. Summary gate — single status check for branch protection
  # =========================================================================
  ci-pass:
    name: "CI ✅"
    runs-on: ubuntu-latest
    needs: [lint, build-frontend, build-backend]
    if: always()
    steps:
      - name: Check all jobs
        run: |
          if [[ "${{ needs.lint.result }}" != "success" ]] || \
             [[ "${{ needs.build-frontend.result }}" != "success" ]] || \
             [[ "${{ needs.build-backend.result }}" != "success" ]]; then
            echo "❌ One or more CI jobs failed"
            exit 1
          fi
          echo "✅ All CI checks passed"
